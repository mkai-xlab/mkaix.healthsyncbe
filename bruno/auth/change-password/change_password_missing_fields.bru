meta {
  name: change_password_missing_fields
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/change-password
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "oldPassword": "password"
  }
}
