meta {
  name: change_password_short_password
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/change-password
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "oldPassword": "password",
    "newPassword": "123"
  }
}
