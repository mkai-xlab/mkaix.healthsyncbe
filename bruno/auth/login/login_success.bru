meta {
  name: Login Success
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "admin12345"
  }
}

vars:post-response {
  accessToken: res.body.accessToken
}