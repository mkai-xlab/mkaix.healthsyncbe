meta {
  name: Edit Invalid Fields
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/doctors/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "email": "invalid-email",
    "degree": "This degree string is way too long and should definitely exceed the one hundred characters limit because I just keep typing and typing and typing and typing and typing and typing and typing..."
  }
}
