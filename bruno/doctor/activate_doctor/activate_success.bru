meta {
  name: Activate Doctor Success
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/doctors/{{doctorId}}/activate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  doctorId: 240
  accessToken: ""
}
