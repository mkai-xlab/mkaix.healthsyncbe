meta {
  name: Activate Doctor Not Found
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/doctors/{{doctorId}}/activate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  doctorId: 99999
  accessToken: ""
}
