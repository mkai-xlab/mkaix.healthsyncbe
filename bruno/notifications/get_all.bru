meta {
  name: Get All Notifications
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/notifications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("returns read and unread notifications", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
