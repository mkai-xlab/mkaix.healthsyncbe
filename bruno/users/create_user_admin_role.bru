meta {
  name: Create User Admin Role (Should Fail)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "fullName": "Admin User",
    "email": "admin01@healthsync.com",
    "phone": "0912345678",
    "roleId": 1
  }
}
