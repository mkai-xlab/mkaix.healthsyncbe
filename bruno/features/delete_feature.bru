meta {
  name: Delete Feature
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/features/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("deletes feature", function() {
    expect(res.status).to.equal(204);
  });
}
