meta {
  name: Delete Permission
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/permissions/22
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("deletes permission", function() {
    expect(res.status).to.equal(204);
  });
}
