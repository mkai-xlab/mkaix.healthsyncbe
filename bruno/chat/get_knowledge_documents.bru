meta {
  name: Get Knowledge Documents
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/knowledge-documents
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("returns ingestion states", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
